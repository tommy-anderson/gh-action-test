name: Inspect Contexts
on:
  pull_request:
    branches:
      - master
jobs:
  build:
    name: Inspect contexts
    runs-on: ubuntu-latest
    steps:
      - name: show context
        run: echo ${{ toJson(github) }}`
      - name: Get Label
        run: echo testing